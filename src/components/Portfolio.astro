---
import TechIcons from "./TechIcons.astro";
---

<style>
  .loader {
    width: fit-content;
    font-weight: bold;
    font-family: monospace;
    clip-path: inset(0 3ch 0 0);
    animation: clipPath 3s steps(4) infinite;
  }

  @keyframes clipPath {
    to {
      clip-path: inset(0 -1ch 0 0);
    }
  }
</style>

<div
  class="flex flex-col max-w-5xl px-4 mx-auto justify-between text-center space-y-2"
>
  <h2 class="text-3xl text-center my-4">Portfolio</h2>
  <pre>coming soon <span class="loader">...</span></pre>
  <TechIcons
    classes="grid grid-cols-4 gap-2 mt-4 md:grid-cols-8 "
    iconClasses="size-5"
  />
</div>
